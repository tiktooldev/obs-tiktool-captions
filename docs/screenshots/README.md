# Screenshots

These are populated after a real build + smoke-test on each OS. Recommended captures (1920x1080 minimum, retina 2880x1800 if available):

1. `01-dock-idle.png` - dock parked on the right side of OBS, status pill `Connecting`.
2. `02-dock-live.png` - dock in live state, level meter active, transcript scrolling.
3. `03-wizard-mic.png` - wizard step 2, mic picker open with several sources.
4. `04-wizard-language.png` - wizard step 3, source language + translation selectors.
5. `05-overlay-in-scene.png` - OBS canvas with the vertical 9:16 caption overlay rendered on top of a TikTok-shaped frame.
6. `06-topup-modal.png` - dock with the topup CTA + browser modal open showing the three packs.

Use captures from the latest OBS Studio stable.
